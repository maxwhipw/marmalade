{
  "name": "marmalade",
  "private": true,
  "version": "0.0.1",
  "description": "Personal-first orchestrator over coding harnesses",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  }
}
